# 构建产物
bin/tesla
bin/tesla.exe
bin/archive.*
dist/
manpages/
completions/

# 编辑器
.idea/
.vscode/
*.swp
*~
.DS_Store

# 本地配置(永远不要提交)
*.env
*.env.local
.env
secrets/
private-key.pem

# Go 临时文件
*.test
*.out
coverage.txt
coverage.html

# 测试缓存
testdata/cache/

# Claude Code 工作目录
.claude/

# 一次性 reproducer / 诊断小程序(Go 工具链已忽略下划线前缀目录,这里再保险一道)
cmd/_*/

# 真车敏感信息黑名单(本地 only,公仓库永不持有真值字符串)
# 测试代码读这个文件扫 testdata/*.json 是否退化回真值;
# 文件不存在时测试自动 t.Skip(CI/fork 不受影响)。
**/.forbidden_strings

